[Unit]
Description=Run the Orbit snapshot builder periodically

[Timer]
# every 2 minutes, plus shortly after boot; jitter avoids hitting the API on the dot
OnBootSec=45
OnUnitActiveSec=120
RandomizedDelaySec=15
AccuracySec=10
Persistent=true

[Install]
WantedBy=timers.target
